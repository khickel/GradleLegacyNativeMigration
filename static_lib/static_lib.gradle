plugins {
    id 'cpp-library'
}

apply from: "${rootDir}/commonNativeUtils.gradle"

library { lib->
    linkage = [Linkage.STATIC]

    binaries.configureEach {
        def compileTask = compileTask.get()
        compileTask.source.from fileTree(dir: ".", include: "*.c")
    }
    publicHeaders.from file('.')
    addDefaultCPPLibraryOptions(project, lib)
    // TODO-KFH temp change to try to figure out what is bringing in DriverSpec.h
//    addVSCompilerOption(project, '/showIncludes')
   
}
