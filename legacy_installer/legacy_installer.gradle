plugins {
    id 'glm.installer-base'
}

installers {
    debug
    release
}
installers.all {
    manifest(project(':subsystem_a:util_1')) {
        from('bin')
    }
    manifest(project(':subsystem_b:server_1')) {
        from('bin')
    }
}